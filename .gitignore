.DS_Store
Thumbs.db
*.log
node_modules/
.netlify/
.vscode/
.idea/
# _deploy/site se commituje (Netlify publish dir s verzemi /1 /2...), zip ne
_deploy/*.zip
# _deploy/prod = produkcni launch build (_launch.ps1) — generuje se az pri ostrem spusteni, necommitovat prubezne
_deploy/prod/
# zaloha screenshotu recenzi ze stareho webu (soukrome, necommitovat do public repa)
_zaloha/

# pracovní round-trip soubory (revize textů od Marka) — nepatří do veřejného repa
TEXTY-K-REVIZI.md
*-REVIZE.md

# velké zdrojové fotky realizací (76 MB) — do gitu jde jen kopie v _deploy/site (kvůli Netlify),
# pracovní složku necommitujeme dvakrát; na disku zůstává pro generátor a preview
/assets/realizace/

# stazena PDF katalogu (pracovni cache, do gitu nepatri)
/_tools/_cache/
